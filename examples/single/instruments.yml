all:
  create_server:
    image: cirros-0.3.5-x86_64-disk
    flavor: m1.micro
    wait_for_instance: no
    timeout_before_floating_ip: 10
    username: cirros
    ansible_user: cirros
    ansible_ssh_pass: cubswin:)
  setup_image:
    image: cirros-0.3.5-x86_64-disk
