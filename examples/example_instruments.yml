all:
  create_server:
    image: cirros
    external_network: public
    flavor: m1.nano
  setup_image:
    image: lala

webservers:
  docker:
    ohoho: alal

databases:
  docker

spark:
  spark

computing:
  r-base:
  r-devel:
